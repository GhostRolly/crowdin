{% extends 'base.html.twig' %}

{% block title %}CrowdinProject{% endblock %}

{% block body %}
    <h1>CrowdinProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ crowdin_project.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ crowdin_project.Name }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ crowdin_project.User }}</td>
            </tr>
            <tr>
                <th>BaseLang</th>
                <td>{{ crowdin_project.BaseLang }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ crowdin_project.Content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crowdin_project_index') }}">back to list</a>

    <a href="{{ path('crowdin_project_edit', {'id': crowdin_project.id}) }}">edit</a>

    {{ include('crowdin_project/_delete_form.html.twig') }}

{% endblock %}
